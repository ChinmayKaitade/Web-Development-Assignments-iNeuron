/* 05. Write a JavaScript Program that Calculates the area of a circle using the Math
Object's properties and methods. */

// Program to Calculate the Area of Circle

const radius = 8;

const area = Math.PI*Math.pow(radius, 2);

console.log(area);